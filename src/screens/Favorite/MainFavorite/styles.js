import {RFValue} from 'react-native-responsive-fontsize';

const styles = {
  image: {width: '100%', height: RFValue(240), marginBottom: RFValue(12)},
  brokenImage: {
    backgroundColor: 'grey',
    width: '100%',
    height: RFValue(240),
    marginBottom: RFValue(12),
  },
};

export default styles;
